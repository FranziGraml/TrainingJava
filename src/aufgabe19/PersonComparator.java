package aufgabe19;

import java.util.Comparator;
/*
public interface PersonComparator implements Comparator<Person> {
    @Override
    public default int compare(Person p1, Person p2) {
        return p1.getAge().compareTo(p2.getAge());
    }
}
*/
